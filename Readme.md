### ATIVIDADE – MySQL (Banco de Dados Relacional)

Este repositório contém a resolução de atividades práticas de **Banco de Dados Relacional** utilizando **MySQL**.  
Em cada atividade foi criado um banco de dados com uma tabela principal, aplicando **abstração** (seleção de atributos essenciais para o contexto) e comandos **DDL/DML**.

#### 📌 Conteúdo do repositório
As atividades foram organizadas em scripts `.sql` (um script por atividade), contendo:
- Criação do banco de dados e da tabela
- Inserção de registros (INSERT)
- Consultas com filtros (SELECT com `WHERE`)
- Atualização de um registro (UPDATE)

---

### ✅ Atividade 1 — Serviço de RH (Colaboradores)
**Objetivo:** Criar um banco de dados para um serviço de RH, registrando colaboradores.

**Requisitos implementados:**
- Tabela `colaboradores` com **5 atributos relevantes** (abstração)
- Inserção de **no mínimo 5 colaboradores**
- `SELECT` com colaboradores com **salário > 2000**
- `SELECT` com colaboradores com **salário < 2000**
- `UPDATE` atualizando um registro

---

### ✅ Atividade 2 — E-commerce (Produtos)
**Objetivo:** Criar um banco de dados para um e-commerce, registrando produtos.

**Requisitos implementados:**
- Tabela `produtos` com **5 atributos relevantes** (abstração)
- Inserção de **no mínimo 8 produtos**
- `SELECT` com produtos com **valor > 500**
- `SELECT` com produtos com **valor < 500**
- `UPDATE` atualizando um registro

---

### ✅ Atividade 3 — Registro Escolar (Estudantes)
**Objetivo:** Criar um banco de dados para registrar informações de estudantes.

**Requisitos implementados:**
- Tabela `estudantes` com **5 atributos relevantes** (abstração)
- Inserção de **no mínimo 8 estudantes**
- `SELECT` com estudantes com **nota > 7.0**
- `SELECT` com estudantes com **nota < 7.0**
- `UPDATE` atualizando um registro

---

### 🛠️ Tecnologias utilizadas
- **MySQL** 
- **Git/GitHub**

---

### ▶️ Como executar (exemplo)
1. Abra seu MySQL (Workbench, terminal, DBeaver etc.)
2. Execute o script `.sql` correspondente à atividade
3. Verifique os resultados dos `SELECT` e do `UPDATE`

---
Atividades desenvolvidas para fins de aprendizado em Banco de Dados Relacional (MySQL).